HTML 为超文本标记语言，专门用来描述网页的语言，标记语言。
Web标准的构成，结构html   表现css   行为js，
<html></html>，标签成对出现，前面开始 后面结束单标签极少 如 <br/>
标签有 包含和并列关系  
<img src="222.png">是输入图片的
<html>是最大的标签
     <head>
<title></title></head>文档的头部
     网页标题
     <body></body>文档的所有内容，界面内容，基本都在这里
</html>
<!DOCTYPE> 文档类型声明，告诉浏览器用的哪个版本html来显示写到整个页面第一行 
html lang="en"  定义为什么网页， en为英文，，zh-CN为中文网页 
<meta charset="UTF-8"> 
  
标签语义  会让文章结构清晰。标题标签  <h1>-<h6>，六个等级，这是成对出现的。，1-6重要性递减， 
一个标题占一行
段落标签，<p></p>    </br>换行，单标签 
文字强调  <strong></strong>  加粗，，，em倾斜，del删除线   ins下划线

<div> <span>  是盒子，没有语义 div超大盒子 自己独占一行  span小

图像标签 和路径   
1 图像标签<img >单标签这是。  <img src="图像URL"alt=""/>  src是必须属性，用于指定路径和文件名
alt  替换文本，显示不出来图片的时候显示这个文字，title 提示文本，鼠标在图片上提示这个消息，src是路径
width是修改图像宽度height是修改高度，修改一个就行，等比例变化，俩都改也行  border是设置边框粗细
这些属性不分前后顺序。
路径，相对路径：下一级的/  上一级../
超链接<a href="路径，也可以是网页链接 和锚点链接，#xx，"></a>  target 是打开方式，_blank是在新的窗口打开
，默认就是当前                                                        和对应的id=“xx”对应


 表格标签，列表标签，表单标签，综合案例，查阅文档。
表格的基本语法，<table>   <tr>   <td>三个组成，  table是定义表格的，tr是定义的表格的行，td用于定义表格
得单元格，必须嵌套在 tr中用，
表头单元格标签，<th> 表格单元格里面的 文字会加粗 居中 加强版的  tr
表格属性    得写到 table标签里面，和 超链接那样似的，<align>表格的对齐方式，有 left center  right      <border>边框
直接加数字、<cellpadding>规定单元格边沿与其内容之间的空白，默认1像素，<cellspacing>单元格之间的空白，默认2像素
width  像素值或者百分比
表格结构标签，<thead> </>圈起来的表示表格的头部区域，  <tbody>表示表格的主体
合并单元格，1跨行合并，rowspan=“”   2 跨列合并，colspan=“”


列表标签，用来布局页面的，
分为 无序列表（重点，），有序列表 和自定义列表
无序列表<ul>   </ul>                                           有序列表，<ol>  里面还是 <li>
 里面包含的项目是  <li> 列表项 1</li>
<ul>里面只能有<li>  <li>里面可以有别的，
自定义列表 （重点）
<dl>                                           
    <dt>名词1<dt>
     <dd>名词1解释1<dd>
    <dd>名词1 解释2<dd>
    </dl>


表单标签          分为，     表单域，表单控件，提示信息，
<form> 表单域，他会把范围内的表单元素信息提交给服务器，
<form action="url 地址 "  method="提交方式" name=“表单域的名称">   </form>
表单控件，   
1。  input输入表单元素   <input type="属性值">  这是单标签，type可以取很多值，
text文本框用户可以在里面输入任何文字
password密码框，用户看不见输入的
radio是单选按钮， checkbox是多选框    等等
input这个输入的表单元素 还有别的属性，name是定义元素的名称，如果是要实现多选一什么的得定义一样的name
    value可以定义 元素的值                    单选按钮和多选框可以设置checked属性，默认打开时就选中
  maxlength  规定输入字段中最大长度   reset是重置，  submit  是提交按钮  会把表单数据发送到服务器，
file 是文件域  上传文件用，，button是普通按钮，后期结合js用
  label 标签  当点击label里面的元素时，就可以将光标转到对应的表单元素中，
2.select 下拉列表，两组标签组成，    
<select>           
   <option> 选项  </option>   </select>  option 中可以定义 selected="selected" 默认选中
3、textarea 文本域元素，可以使用多行文本输入，
<textarea>   </textarea>      属性，rows=“显示多少行”  cols=“每行的字符数”

                                                                             
